import { useEffect, useState } from 'react';
import { Bell, DatabaseBackup, Globe2, LogOut, Moon, ShieldCheck, SunMedium, UserRound } from 'lucide-react';
import { Button, Card } from '../components/ui';
const defaultPreferences = { language: 'English', theme: 'light', notifications: true, serviceWindow: '08:00 - 18:00' };
function loadPreferences() { try { return { ...defaultPreferences, ...JSON.parse(sessionStorage.getItem('sav-settings-preferences') || '{}') }; } catch { return defaultPreferences; } }
export function SettingsPage({ notify, user, onLogout }) { const [preferences, setPreferences] = useState(loadPreferences); useEffect(() => { sessionStorage.setItem('sav-settings-preferences', JSON.stringify(preferences)); document.documentElement.dataset.theme = preferences.theme === 'dark' ? 'dark' : 'light'; }, [preferences]); function updatePreference(key, value) { setPreferences(current => ({ ...current, [key]: value })); } return <section className="page-shell settings-page"><div className="page-title-row"><div><h1>Settings</h1><p>Workspace preferences and display options.</p></div>{onLogout && <Button icon={LogOut} onClick={onLogout}>Logout</Button>}</div><div className="settings-grid"><Card title="Preferences" icon={Globe2} className="settings-card"><div className="settings-form"><label className="settings-field"><span>Language</span><select value={preferences.language} onChange={event => updatePreference('language', event.target.value)}><option>English</option><option>French</option></select></label><div className="settings-field"><span>Theme mode</span><div className="theme-choice" role="group" aria-label="Theme mode"><button type="button" className={preferences.theme === 'light' ? 'active' : ''} onClick={() => updatePreference('theme', 'light')}><SunMedium size={17} />Light</button><button type="button" className={preferences.theme === 'dark' ? 'active' : ''} onClick={() => updatePreference('theme', 'dark')}><Moon size={17} />Dark</button></div></div><label className="settings-field"><span>Service window</span><input value={preferences.serviceWindow} onChange={event => updatePreference('serviceWindow', event.target.value)} /></label></div></Card><Card title="Notifications" icon={Bell} className="settings-card"><div className="settings-toggle-row"><div><strong>Operational alerts</strong><p>Receive SLA, appointment and assignment updates.</p></div><button type="button" className={`toggle-switch ${preferences.notifications ? 'on' : ''}`} onClick={() => updatePreference('notifications', !preferences.notifications)} aria-pressed={preferences.notifications}><span /></button></div><div className="settings-info-list"><Info label="SLA alerts" value="Enabled for high priority cases" /><Info label="Daily summary" value="Sent every morning" /><Info label="Planning updates" value="Technician changes and visit reminders" /></div></Card><Card title="Account" icon={UserRound} className="settings-card"><div className="settings-profile"><div><strong>{user?.name || 'SAV Pro user'}</strong><span>{user?.role || 'Back office'}</span></div><ShieldCheck size={30} /></div><div className="settings-info-list"><Info label="Workspace" value="SAV Pro" /><Info label="Access level" value={user?.role || 'Operator'} /><Info label="Session" value="HttpOnly cookie session" /></div></Card><Card title="Backend Data" icon={DatabaseBackup} className="settings-card"><p className="settings-card-copy">Business data is now controlled by the backend databases. Local demo reset has been removed.</p><Button type="button" variant="primary" onClick={() => notify('Reset must be done through backend seed/migration scripts.', 'error')}>Backend only</Button></Card></div></section>; }
function Info({ label, value }) { return <div className="settings-info-item"><span>{label}</span><strong>{value}</strong></div>; }
